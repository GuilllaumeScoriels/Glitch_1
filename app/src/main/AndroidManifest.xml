<?xml version="1.0" encoding="utf-8"?>
<!--
================================================================================
ANDROID MANIFEST — RÔLE DANS L’APPLICATION
================================================================================

Le fichier AndroidManifest.xml est le contrat officiel entre l’application et
le système Android. Il est lu AVANT l’exécution du code Kotlin et sert à
déclarer au système :

1) Les composants de l’application (activités, services, providers, receivers)
2) Les permissions autorisées
3) Le point de lancement de l’application
4) Les interactions possibles avec les autres applications
5) Les capacités système utilisées

Sans ce fichier, Android ne peut pas exécuter l’application.


Pourquoi un FileProvider est déclaré ci-dessous ?

L’application permet d’exporter un planning au format calendrier (.ics) afin
de l’ouvrir dans Google Calendar ou toute autre application de calendrier.

Depuis Android 7+, une application n’a plus le droit de partager directement
un fichier interne avec une autre application via un chemin de fichier brut
(file://...). Cela provoquerait un crash :
FileUriExposedException.

Le FileProvider agit comme un intermédiaire sécurisé :

    App → FileProvider → Google Calendar

(URI = Uniform Ressource Identifier, permert d'identifier univoquement une ressource)
(Sandbox android = syst. de sécurité qui isole chaque application dans son environnement d'exécution)
Il génère une URI temporaire sécurisée (content://) autorisée uniquement pour
la lecture par l’application cible. Cela respecte le sandbox Android et empêche
l’accès aux fichiers privés de l’application.

Sans ce provider :
- le partage du fichier .ics échouerait
- Google Calendar ne pourrait pas importer le planning

Ce provider est donc indispensable pour l’export calendrier.
================================================================================
-->

<manifest package="com.example.a18"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name="com.example.lecturemotparmotapp.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._18"
        tools:targetApi="31">

        <activity
            android:name="com.example.lecturemotparmotapp.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme._18">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Déclencheur des notifications au début des créneaux -->
        <receiver
            android:name="Notifications.EventAlarmReceiver"
            android:exported="false" />

        <!-- Replanifie après reboot / mise à jour -->
        <receiver
            android:name="Notifications.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Provider transforme l'app en serveur sécurisé de fichiers temporaire
        Sert à autoriser légalement le partage du fichier .ics
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
